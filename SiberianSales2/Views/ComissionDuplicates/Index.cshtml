@model IEnumerable<SiberianSales2.Models.ComissionDuplicate>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ComissionDuplicateValue)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AvaliableDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ComissionStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SalesOrder)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ComissionDuplicateValue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AvaliableDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ComissionStatus.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SalesOrder.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
