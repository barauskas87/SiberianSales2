@model IEnumerable<SiberianSales2.Models.PaymentTerms>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PaymentTerm)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaymentTermDays)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaymentDuplicates)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PaymentTerm)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaymentTermDays)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PaymentDuplicates)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
